# Generate models in the browser and host on GitHub Pages

The user confirmed that SVG processing, geometry generation, preview, and 3MF export should run entirely in the browser, with the app hosted on GitHub Pages. This keeps uploaded files local and avoids operating a backend, at the cost of requiring browser-compatible geometry tooling and working within client memory and processing limits. A server-based geometry pipeline is outside the agreed architecture.
